import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  registerStyle: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  passwordStyle: {
    flexDirection: 'row',
    alignItems: 'center',
},
  touchStyle: {
    flex: 1,
  },
});
