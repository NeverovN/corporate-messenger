import React, { memo } from 'react';
import { View } from 'react-native';
import InputBar from './InputBar';

const AuthScreen: React.FC = memo((props) => {
  return (
    <View>
      <InputBar navigation={} />
    </View>
  );
});

export default AuthScreen;
