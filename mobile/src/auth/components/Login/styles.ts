import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  loginStyle: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  passwordStyle: {
    flexDirection: 'row',
    alignItems: 'center',
  },
});
