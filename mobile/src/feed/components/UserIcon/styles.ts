import { makeUseStyles } from 'react-native-stylex';

export const useStyles = makeUseStyles(() => {
  return {
    userIconImageStyle: {
      width: 40,
      height: 40,
      borderRadius: 20,
      marginRight: 15,
    },
  };
});
