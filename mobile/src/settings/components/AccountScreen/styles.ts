import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  accountScreenStyle: {
    flex: 1,
    alignItems: 'center',
    marginTop: 30,
  },
  textStyle: {
    marginBottom: 20,
    fontSize: 20,
  },
});
