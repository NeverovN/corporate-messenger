import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  unreadStyle: {
    marginRight: 10,
    borderRadius: 20,
    backgroundColor: 'red',
    alignItems: 'center',
    minWidth: 25,
    padding: 5,
  },
  wrapperStyle: {
    flex: 1,
    alignItems: 'flex-end',
  },
  textStyle: {
    color: 'white',
  },
});
