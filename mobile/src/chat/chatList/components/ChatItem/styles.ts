import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  chatItemStyle: {
    height: 70,
    backgroundColor: '#C4C7CD',
    borderRadius: 10,
    marginTop: 10,
    width: '100%',
    flexDirection: 'row',
    alignItems: 'center',
  },
});
