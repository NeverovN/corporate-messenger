import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  existingChatsStyle: {
    marginHorizontal: 5,
    height: '100%',
  },
  missingChatsStyle: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  textStyle: {
    fontSize: 20,
  },
});
