import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  userNameViewStyle: {
    width: '100%',
    height: 20,
    justifyContent: 'center',
    // marginTop: 10,
    marginBottom: 5,
  },
});
