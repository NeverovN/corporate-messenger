import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  profileStyle: {
    // flex: 1,
    marginHorizontal: 5,
  },
  flatListStyle: {
    flexDirection: 'column-reverse',
  },
});
