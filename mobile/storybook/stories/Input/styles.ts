import { StyleSheet } from "react-native";

export default StyleSheet.create({
  default: {
    width: 200,
    height: 30,
  },
  withBorder: {
    borderWidth: 1,
    borderRadius: 10,
    borderColor: 'black',
  },
})