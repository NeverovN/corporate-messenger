type Query {
  getPosts(author: ID): [Post]
  getPost(id: ID!): Post
  getAllPosts: [Post]
  getFriendPosts: [Post]
}

type Subscription {
  newPost: Post!
}

type Subscription {
  newPost: Post!
}

type Mutation {
  createPost: Post!
  getUsersPosts: [Post]
  getPost(id: ID!): Post
}

type Post {
  id: ID!
  author: User!
  createdAt: String!
  lastEdit: String
  comments: [CommentModel]
  textContent: String
  mediaContent: [String]
}

type CommentModel {
  id: ID!
  author: User!
  createdAt: String!
  lastEdit: String
  likes: [User]
}
