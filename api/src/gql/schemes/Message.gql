type Message {
  id: ID!
  content: String!
  author: User!
  receivers: [User!]!
  createdAt: String!
  lastEdit: String
}

type Query {
  getMessages: [Message]
}

type Mutation {
  createMessage(content: String!, chatId: String!): Message!
}

type Subscription {
  newMessage: Message!
}
