type Chat {
  id: ID!
  participants: [User]!
  logo: String
  # messages: [ChatMessage]
  settings: ChatSettings
}

type ChatSettings {
  testField: String
}

type Query {
  getChats: [Chat]
}

type Mutation {
  createChat: Chat!
}

type Subscription {
  newChat: Chat!
}
